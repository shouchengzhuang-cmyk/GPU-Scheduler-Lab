# Alibaba trace fixture

These rows are the public schema examples shown in Alibaba's
`cluster-trace-v2026-spot-gpu` README. They are committed only as a small parser
fixture. The complete dataset is not redistributed by this repository.
